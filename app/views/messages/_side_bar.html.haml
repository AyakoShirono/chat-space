.Side-bar
  .Side-bar__user-name
    .Side-bar__user-name__top-items
      %h3.Side-bar__user-name__top-items__name
        = current_user.name
      .Side-bar__user-name__top-items__icon
        = link_to new_group_path do
          = icon('far', 'edit', class: "Side-bar__user-name__top-items__icon__btn")
          %span<>
        = link_to edit_user_path(current_user) do
          = icon('fa', 'cog', class: "Side-bar__user-name__top-items__icon__btn")
          %span<>
  .Side-bar__group-list
    - current_user.groups.each do |group|
      .Side-bar__group-list__items
        = link_to '#' do
          .Side-bar__group-list__items__title
            = group.name
          .Side-bar__group-list__items__message
            まだメッセージはありません
    